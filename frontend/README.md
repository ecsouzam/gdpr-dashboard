# GDPR Dashboard — Frontend

React + Vite + Tailwind CSS dashboard UI. See the [project root README](../README.md) for full setup and run instructions.

Quick start: `./run.sh` (or `npm install && npm run dev`).
